using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class MainCameraCode : MonoBehaviour
{
    [SerializeField] private CinemachineVirtualCamera cinecahineVirtualCamera;

    private void Update()
    {
        {
            if (Input.GetKeyDown(KeyCode.T))
            {
               
            }
        }
    }
}
